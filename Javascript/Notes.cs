Notes
---------
There are 3 ways , er can add javascript to a html file

inline: 
internal: <script> inside head, body,
end of body
external: script tag


Single threaded prog:
 Execution Context
How to declare veriable in javascript
1. var
2. let
3. const

Difference between var , let, const with execution Context Data Type
In js data type are dynamic
Number
String
Boolean
Null
Undefined
Object: array, object, date
Typeof veriable
 string in JavaScript must be surrounded by quotes.
  JavaScript, there are 3 types of quotes.

Double quotes: "Hello".
Single quotes: 'Hello'.
Backticks: `Hello`.


DataType :
===================

2 types of datatypes

  1. Primitive datatypes
      Number 12.30
      console.log(a)
      String:
        Double quotes: "Hello".
        Single quotes: '12'.
        Backticks: `Hello`.
      Boolean: true, false
      Null
      Undefined
  2. Non-primitive datatypes
      object: object, array, date

      Statements
      ==============
      conditional Statements
        if, switch
      iteration:
        for, while , do while, for in, for of
      jump
      exception handling 

      if Statements
      ===================
      // one condition
      if (condition)
        code;
      else 
        code;

==,===

5 === 5 false
5 == '5' true
 +=

 var a = 10;
 a+=20
 console.log(a)

 var count = 0;
 count++;
 count--;
 console.log(count)


     // two condition
    if (condition){
        code;
      } else if (condition2){
        conde;
      } else if (condition2){
        conde;
      }

      // else
    if (condition){
        code;
      } else if (condition){
        code;
      } else {
        code;
      }

    // nested if
      if (condition){
        if (condition 2){
          if (condition 3){
            
          }
        }
      }
expression = 2;
   switch (expression){
    case 1:
      code ;
      break;
    case 2:
      code ;
      break;
   }


expression1 = 'A';
   switch (expression){
    case 'A':
      code ;
      break;
    case 'B':
      code ;
      break;
   }

How to Open Github Account;
download and install git from https://git-scm.com/
step1 : open https://github.com/
step2 : click on sign up option
step3 : after setup done : git clone repository name

<h1>Loops in Javascript</h1>
    <ul>
        <li>for</li>
        <li>for in</li>
        <li>for of</li>
        <li>foreach</li>
        <li>while</li>
        <li>do while</li>

for (initialization;condition;increment){
  code
}



